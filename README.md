check the branch and then push the code

BEFORE WORK:
git clone <repo-url>
cd SecurePay
git checkout frontend-yamuna

AFTER WORK:
git add .
git commit -m "Added Login and Register pages"
git push origin frontend-yamuna



PERSON 2:

BEFORE WORK:
git clone <repo-url>
cd SecurePay
git checkout frontend-elakkiya

AFTER WORK:
git add .
git commit -m "Added Dashboard UI"
git push origin frontend-elakkiya

